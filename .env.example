# Copia este archivo a .env y completa tu cadena de conexión de PostgreSQL
# Formato recomendado (Railway/Render/Supabase):
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public"
DATABASE_URL="postgresql://user:password@localhost:5432/proyecto_ropa?schema=public"

# NextAuth (requerido para login de admin con credenciales)
NEXTAUTH_SECRET="cambia-esto-por-una-cadena-aleatoria-segura"
ADMIN_EMAIL="admin@tu-dominio.com"
# Por simplicidad en desarrollo, usamos contraseña en texto plano.
# En producción cambiaremos a hash (bcrypt) más adelante.
ADMIN_PASSWORD="cambia-esta-contraseña"

# Stripe
STRIPE_SECRET_KEY="sk_test_xxx"
# Moneda a usar en Stripe (por defecto USD). Asegúrate de que los precios estén en centavos
# y que la moneda esté habilitada en tu cuenta Stripe.
STRIPE_CURRENCY="USD"

# WhatsApp (E.164 sin signos, ej: 573001234567)
NEXT_PUBLIC_WHATSAPP_PHONE="573005628380"

# Cloudinary (para subir imágenes de productos)
# Crea una cuenta en https://cloudinary.com/, luego ve a Dashboard y copia estos valores.
# Si prefieres otro proveedor (Supabase Storage, S3), ignora estas variables.
CLOUDINARY_CLOUD_NAME="tu_cloud_name"
CLOUDINARY_API_KEY="tu_api_key"
CLOUDINARY_API_SECRET="tu_api_secret"
# Opcional: carpeta en Cloudinary donde guardar las imágenes
CLOUDINARY_UPLOAD_FOLDER="moda-celestial/products"

# Supabase (alternativa 100% gratis para BD + Storage de imágenes)
# Crea un proyecto en https://supabase.com/ y copia estos valores desde Project Settings.
# Si usas Supabase, NO necesitas Cloudinary. Usa el bucket "products" en Supabase Storage.
SUPABASE_URL="https://<your-project-ref>.supabase.co"
SUPABASE_ANON_KEY="<supabase-anon-key>"
# Solo del lado servidor (NUNCA en el cliente)
SUPABASE_SERVICE_ROLE_KEY="<supabase-service-role>"
# (Opcional) URL de conexión Postgres vía pooler (recomendado para serverless)
# Formato: postgresql://USER:PASSWORD@HOST:6543/postgres?sslmode=require
SUPABASE_DB_POOLER_URL="postgresql://postgres:<db-password>@db.<project-ref>.supabase.co:6543/postgres?sslmode=require&pgbouncer=true&connection_limit=1"